#comment
  .user-name
    = comment.user.username
  .comment-content
    = comment.content
  - if comment.user == current_user
    = link_to snap_comment_path(snap, comment), method: :delete, data: { confirm: "Are you sure?"}, remote: true do
      %span(class="glyphicon glyphicon-remove delete-comment")